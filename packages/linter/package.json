{
  "name": "@htmlslide/linter",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc -b tsconfig.json --pretty false",
    "test": "vitest run --root ../.. --config vitest.config.ts packages/linter",
    "typecheck": "tsc -b tsconfig.json --pretty false"
  },
  "dependencies": {
    "@htmlslide/core": "workspace:*"
  }
}
